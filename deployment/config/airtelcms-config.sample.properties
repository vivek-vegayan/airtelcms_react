# ============================================================================
# CHM Airtel Scheduler - external runtime configuration (TEMPLATE)
# Loaded by AppPropertiesConfig at backend startup. THE BACKEND WILL NOT START
# WITHOUT THIS FILE.
#
#   Windows : C:\vegayan\simplus\airtelcms-config.properties
#   Linux   : /home/vegayan/simplus/config_airtel.properties
#
# Fill real values and restrict file permissions (Linux: chmod 640, owner chm).
# ============================================================================

# ---- User-management / RBAC database ----
DBSOURCE_USERMGMT_IP=127.0.0.1
DBSOURCE_USERMGMT_USER=chm_app
DBSOURCE_USERMGMT_PASS=CHANGE_ME
DBSOURCE_USERMGMT_DBNAME=usermgmt_db

# ---- Primary application database ----
DBSOURCE1_IP=127.0.0.1
DBSOURCE1_USER=chm_app
DBSOURCE1_PASS=CHANGE_ME
DBSOURCE1_DBNAME=chm_db

# ---- SSH host 1 (script execution: shift generation, checkpoint refetch, PDF) ----
SSH_HOST=
SSH_PORT=22
SSH_USERNAME=
SSH_PASSWORD=
SSH_SCRIPT_PATH=
SSH_REFETCH_CHECKPOINT_SCRIPT_PATH=
SSH_SHIFT_GEN_VAL_SCRIPT_PATH=
SSH_PDF_VIEW_DOWNLOAD_PATH=

# ---- SSH host 2 ----
SSH2_HOST=
SSH2_PORT=22
SSH2_USERNAME=
SSH2_PASSWORD=
SSH2_SCRIPT_PATH=

# ---- SFTP (file exchange: CRQ worklog files, raw JSON) ----
SFTP_HOST=
SFTP_USERNAME=
SFTP_PASSWORD=
SFTP_FILE_PATH=
SFTP_JSON_RAW_FILE_PATH=
SFTP_FILE_PATH_CRQ_WEORKLOG_FILE=
